# Tech Context

Next.js 16 + React 19 + Tailwind v4 + sharp + jszip + fluent-ffmpeg + ffmpeg-static + xlsx + pdfkit + mammoth + pdf-parse.
No env/API keys required. Video upload body limit 500MB (`proxyClientMaxBodySize`). Docs ≤50MB/file.
Optional: `NEXT_PUBLIC_SITE_URL` for canonical / sitemap / OG URLs (default `https://pixora.app`).
